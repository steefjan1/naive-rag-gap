# azure-search-documents 12.0.0 is the first GA release that ships the
# knowledgebases module, so all four gaps - including agentic retrieval -
# run on the stable package. No --pre needed.
azure-search-documents>=12.0.0
azure-identity>=1.19.0
openai>=1.60.0
python-dotenv>=1.0.0

# Preview-only agentic features (query planning, answer synthesis,
# configurable reasoning effort) still require the beta:
#   pip install --pre azure-search-documents
